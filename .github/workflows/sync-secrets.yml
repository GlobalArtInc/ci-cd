# Sync secrets to all repositories in the organization.
# Usage: run manually via workflow_dispatch.
#
# Required secrets in this repo:
#   GH_TOKEN         — PAT with scopes: repo, admin:org (GlobalArtInc)
#   GH_TOKEN_OWNLATE — PAT with scopes: repo, admin:org (OwnLate)
#
# Example:
#   KEYS: HARBOR_HOST,HARBOR_USERNAME,HARBOR_PASSWORD,KUBECTX_PROD1,KUBECTX_STAGE1,WERF_SECRET_KEY

on:
  workflow_dispatch:
    inputs:
      KEYS:
        required: true
        type: string
        default: "HARBOR_HOST,HARBOR_USERNAME,HARBOR_PASSWORD,KUBECTX_PROD1,KUBECTX_STAGE1,WERF_SECRET_KEY"
        description: "Comma-separated secret names to sync"

jobs:
  sync_globalartinc:
    uses: ./.github/workflows/sync-secrets-org.yml
    with:
      org: GlobalArtInc
      keys: ${{ inputs.KEYS }}
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
      HARBOR_HOST: ${{ secrets.HARBOR_HOST }}
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
      KUBECTX_PROD1: ${{ secrets.KUBECTX_PROD1 }}
      KUBECTX_STAGE1: ${{ secrets.KUBECTX_STAGE1 }}
      WERF_SECRET_KEY: ${{ secrets.WERF_SECRET_KEY }}

  sync_ownlate:
    uses: ./.github/workflows/sync-secrets-org.yml
    with:
      org: OwnLate
      keys: ${{ inputs.KEYS }}
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN_OWNLATE }}
      HARBOR_HOST: ${{ secrets.HARBOR_HOST }}
      HARBOR_USERNAME: ${{ secrets.HARBOR_USERNAME }}
      HARBOR_PASSWORD: ${{ secrets.HARBOR_PASSWORD }}
      KUBECTX_PROD1: ${{ secrets.KUBECTX_PROD1 }}
      KUBECTX_STAGE1: ${{ secrets.KUBECTX_STAGE1 }}
      WERF_SECRET_KEY: ${{ secrets.WERF_SECRET_KEY }}
